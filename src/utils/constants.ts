/**
 * @fileoverview Constants for bkoi-gl-js
 * @description Default values used across the library for maps and controls.
 */

/**
 * Default map center coordinates (Dhaka, Bangladesh).
 * Used when no center is specified or as a fallback for minimap initial positioning.
 *
 * @type {[number, number]}
 */
export const DEFAULT_CENTER: [number, number] = [90.39017821904588, 23.719800220780733]
